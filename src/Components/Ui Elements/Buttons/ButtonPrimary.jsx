

const ButtonPrimary = ({text}) => {
    return (
        <div className={`bg-primary py-[17px] px-[20px] rounded-[50px] text-center text-secondary text-sm font-bold `}>
           {text}
            
        </div>
    );
};

export default ButtonPrimary;