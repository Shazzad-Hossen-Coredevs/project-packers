

const Input = ({type, placeholder}) => {
  return (
    <div className="flex align-middle  border border-secondary">

        <input className="" type={type} placeholder={placeholder} />
    </div>
  );
};

export default Input;
